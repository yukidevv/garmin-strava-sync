[project]
name = "garmin-strava-sync"
version = "0.1.0"
description = "Sync Garmin run title/description changes to Strava"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "garminconnect>=0.3.3",
    "python-dotenv>=1.2.2",
    "requests>=2.32.0",
    "fastapi>=0.115.0",
    "uvicorn>=0.34.0",
]
