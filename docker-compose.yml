services:
  # 単発ジョブ。常駐せず、起動するたびに1回同期して終了する。
  # スケジューリングはホストのsystemd timerが `docker compose run --rm sync` を毎時実行（systemd/ 参照）。
  sync:
    build: .
    env_file: .env
    environment:
      GARMIN_TOKEN_DIR: /data/.garminconnect
    volumes:
      - garmin_token:/data

volumes:
  garmin_token:
